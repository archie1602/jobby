namespace Jobby.Core.Models.Dashboard;

// English must remain the zero value for DTOs that omit the language field.
public enum DashboardLanguage
{
    English = 0,
    Russian = 1,
    PortugueseBrazil = 2,
}
